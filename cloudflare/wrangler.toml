name = "nightdesk-dashboard"
pages_build_output_dir = "public"
compatibility_date = "2024-09-23"

# Public URL of the deployed NightDesk FastAPI backend (app/server.py).
# Set this before deploying (non-secret var is fine here; use `wrangler secret put`
# if you prefer it hidden):
[vars]
BACKEND_URL = "https://nightdesk.example.com"
